{
  "builders": [
    {
      "type": "virtualbox-iso",
      "disk_size": "{{ user `disk_size` }}",
      "floppy_files": [
        "http/{{ user `preseed` }}"
      ],
      "guest_additions_path": "VBoxGuestAdditions_{{.Version}}.iso",
      "guest_os_type": "{{ user `virtualbox_guest_os_type` }}",
      "hard_drive_interface": "sata",
      "headless": "{{ user `headless` }}",
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_checksum_type": "{{ user `iso_checksum_type` }}",
      "iso_urls": [
        "{{ user `iso_url` }}"
      ],
      "output_directory": "output-{{ user `vm_name` }}-virtualbox-iso",
      "shutdown_command": "echo '{{ user `ssh_password` }}'|sudo -S shutdown -P now",
      "ssh_password": "{{ user `ssh_password` }}",
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_wait_timeout": "10000s",
      "vboxmanage": [
        [ "modifyvm", "{{.Name}}", "--memory", "{{ user `memory` }}" ],
        [ "modifyvm", "{{.Name}}", "--cpus", "{{ user `cpus` }}" ]
      ],
      "virtualbox_version_file": ".vbox_version",
      "vm_name": "{{user `vm_name`}}",
      "boot_command": [
        "<esc><esc><enter><wait>",
        "/install/vmlinuz noapic ",
        "file=/floppy/{{ user `preseed` }} ",
        "debian-installer=en_US auto locale=en_US kbd-chooser/method=us ",
        "hostname={{ user `hostname` }} ",
        "fb=false debconf/frontend=noninteractive ",
        "keyboard-configuration/modelcode=SKIP keyboard-configuration/layout=USA ",
        "keyboard-configuration/variant=USA console-setup/ask_detect=false ",
        "passwd/user-fullname={{ user `ssh_fullname` }} ",
        "passwd/user-password={{ user `ssh_password` }} ",
        "passwd/user-password-again={{ user `ssh_password` }} ",
        "passwd/username={{ user `ssh_username` }} ",
        "initrd=/install/initrd.gz -- <enter>"
      ]
    },
    {
      "type": "virtualbox-ovf",
      "source_path": "{{ user `cache`}}/box/vagrant.ovf",
      "headless": "{{ user `headless` }}",
      "output_directory": "output-{{ user `vm_name` }}-virtualbox-ovf",
      "shutdown_command": "echo '{{ user `ssh_password` }}'|sudo -S shutdown -P now",
      "ssh_password": "{{ user `ssh_password` }}",
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_wait_timeout": "10000s",
      "vboxmanage": [
        [ "modifyvm", "{{.Name}}", "--memory", "{{ user `memory` }}" ],
        [ "modifyvm", "{{.Name}}", "--cpus", "{{ user `cpus` }}" ]
      ],
      "vm_name": "{{user `vm_name`}}"
    },
    {
      "type": "amazon-ebs",
      "access_key": "{{ user `access_key` }}", 
      "secret_key": "{{ user `secret_key` }}", 
      "source_ami": "{{ user `image_id` }}",
      "region": "{{ user `region` }}",
      "instance_type": "{{ user `instance_type` }}",
      "ssh_username": "{{ user `ssh_username` }}",
      "ami_name": "{{user `snapshot_name`}}"
    },
    {
      "type": "docker",
      "image": "ubuntu:latest",
      "commit": "true",
      "pull": "false"
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "only": [ "virtualbox-iso", "virtualbox-ovf", "amazon-ebs"  ]
      "keep_input_artifact": false,
      "output": "{{user `cache`}}/box/{{user `provider`}}/{{user `vm_name`}}.box",
      "vagrantfile_template": "{{ user `vagrantfile_template` }}"
    },
    {
      "type": "docker-tag",
      "only": [ "docker" ],
      "repository": "ubuntu",
      "tag": "packer",
      "force": "true"
    }
  ],
  "provisioners": [
    {
      "environment_vars": [
        "CLEANUP_PAUSE={{user `cleanup_pause`}}",
        "DOCKER={{user `docker`}}",
        "UPDATE={{user `update`}}",
        "INSTALL_VAGRANT_KEY={{user `install_vagrant_key`}}",
        "SSH_USERNAME={{user `ssh_username`}}",
        "SSH_PASSWORD={{user `ssh_password`}}"
      ],
      "execute_command": "echo '{{ user `ssh_password` }}' | {{.Vars}} sudo -E -S bash '{{.Path}}'",
      "scripts": [ ],
      "type": "shell"
    }
  ],
  "variables": {
    "cleanup_pause": "",
    "cpus": "1",
    "custom_script": ".",
    "disk_size": "65536",
    "docker": "false",
    "headless": "true",
    "install_vagrant_key": "false",
    "iso_checksum": "1d1ff8df8c3182af3192e979317f28dbfb148f50",
    "iso_checksum_type": "sha1",
    "iso_url": "http://cdimage.ubuntu.com/releases/14.04/release/ubuntu-14.04.3-server-amd64+mac.iso",
    "memory": "512",
    "preseed" : "preseed.cfg",
    "hostname": "vagrant",
    "ssh_fullname": "ubuntu",
    "ssh_password": "ubuntu",
    "ssh_username": "ubuntu",
    "update": "false",
    "vagrantfile_template": "",
    "virtualbox_guest_os_type": "Ubuntu_64",
    "vm_name": "ubuntu1404",
    "image_id": "",
    "instance_type": "",
    "cache": "{{ env `BASEBOX_CACHE`}}" }
}

